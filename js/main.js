$("document").ready(function($) {
    $(function() {

        $(".flipster").flipster({
            style: 'carousel',
            enableNav: false,
        });

    });
    // navbar change background
    // var nav = $('#navbar');
    // $(window).scroll(function() {

    //     if ($(this).scrollTop() > 0) {
    //         nav.addClass("sticky");
    //     } else {
    //         nav.removeClass("sticky");
    //     }
    // });
})